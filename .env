# Local dev secrets — loaded by app/config.py (SettingsConfigDict(env_file=".env")).
# Not present in a fresh checkout; test-ui.html/chat-widget.js ship with this
# exact key hardcoded for local demo use, so the backend needs to match it.
API_SECRET_KEY=sk-23189ec2657d68794d7b680c04b5079e57d2e892e34a863c
